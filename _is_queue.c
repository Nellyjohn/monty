#include "monty.h"

/**
 * _is_queue - prints the top
 * @h: stack head
 * @line_number: line number
 */

void _is_queue(stack_t **h, unsigned int line_number)
{
	(void)h;
	(void)line_number;

	var.queue = QUEUE;
}
