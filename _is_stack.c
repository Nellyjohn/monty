#include "monty.h"
/**
 * _is_stack - prints the top
 * @h: stack head
 * @line_number: line_number
 * Return: no return
*/
void _is_stack(stack_t **h, unsigned int line_number)
{
	(void)h;
	(void)line_number;
	var.queue = STACK;
}
