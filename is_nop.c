#include "monty.h"

/**
  * _is_nop - doesn’t do anything
  * @h: head of the list
  * @line_number: bytecode line number
  * Return: nothing
  */
void _is_nop(stack_t **h, unsigned int line_number)
{
	(void)h;
	(void)line_number;
}
